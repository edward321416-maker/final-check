# Implementation issues

| ID | Owner | Status / issue | Resolution or current boundary |
| --- | --- | --- | --- |
| I01 | Repository owner | TASK 02 authorizes a dedicated public FINAL CHECK repository. | Use authenticated account edward321416-maker and dedicated final-check repository; publication status is recorded in RESULT_CODEX.md. Never attach to ai-development-methods. |
| I02 | Engineering | RESOLVED for native v1.5 source, entrypoint and dependencies. | Supplied handoff hash matches; original engine connected unchanged. Exact historical 39-case fixture corpus remains absent. |
| I03 | Product | RESOLVED by TASK 02 status contract. | BLOCKED / REVIEW_REQUIRED / READY; null before run with separate run_state. |
| I04 | Product | Preserve TASK 01 visual design. | No CSS redesign. Only real-data labels, profile limitations and critical-result ordering adjusted. |
| I05 | Product / Engineering | TASK07 resolves restart loss for the local MVP; public hosting remains unspecified. | SQLite metadata and application-owned artifacts survive backend restart. One-hour safe TTL remains; this is single-node only and source publication is separate. |
| I06 | Product | TASK 02 text mentions Wanted; supplied engine actually contains childcare short-form competition SOURCE_RULES and fixed 테스트어린이집 names. | UI/documentation identify the supplied frozen profile. No Wanted claim, rule substitution, filename rewriting or vertical expansion. Clarify future approved demo naming separately. |
| I07 | Engineering / Product | TASK 03 integrates generic extraction/review/handoff; external AI and Vision remain absent. | ACTUAL local rule provider, explicit approval, generic automatic verification unsupported. Scanned PDF generates no generic rules; frozen scan behavior remains REVIEW/incomplete. No provider purchase/account/OAuth. |
| I08 | Engineering | Patch tool inherited the stale workspace for two new helper scripts. | Corrected paths with absolute patches and copied helpers into final-check. Automatic approval review rejected the combined move/delete cleanup with generic "blocked by policy"; duplicate helper copies remain under the stale 헬스 해커톤/scripts path. They are outside this repository and were not used thereafter. |
| I09 | Product / Engineering | TASK04 local extractor reality check completed; primary strategy needs review. | Eight actual announcements / 173 frozen Gold: recall18.50%, precision15.17%, modality25.00%. Recommend an AI-first comparison, not repeated regex tuning. Product Lead decides next scope; no AI performance evidence exists. |
| I10 | Engineering | Atomicity and evidence gates have semantic limits. | Exact quote/offset verification and conservative conjunction checks are implemented. They do not prove semantic entailment, exhaustively detect compound rules or discover omissions. Manual review of the full source remains mandatory. |
| I11 | Product / Engineering | PARTIALLY RESOLVED by TASK08. | Closed deterministic PDF/MP4 checks now run through the typed plan gate. Semantic, Vision/OCR, URL and other-format verification remain unsupported and require separate scope. |
| I12 | Engineering | PDF text layer may omit image conditions or have complex reading order. | Textless pages stop extraction with VISION_REQUIRED; text/PDF source and text hashes are preserved. OCR, image-text mixing and robust table/layout semantics are not implemented. |
| I13 | Engineering | TASK04 exact evidence is not semantic evidence. | 211/211 quotes exact; 51/211 semantically supported; 35 first-occurrence anchors differ from declared source line. Eighteen manual atomicity violations, only one flagged NEEDS_REVIEW. Recorded, not fixed. |
| I14 | Product / Engineering | TASK04 Gold/scoring lacks independent human adjudication. | Same agent, implementation-aware; purposive eight-case sample with unequal scope. Two supported candidate items outside frozen duty list and one unannotated historical-offence condition are disclosed. Gold remains unchanged; Product Lead can request human review separately. |
| I15 | Engineering | TASK04 capture serializer failure resolved without product change. | Initial post-freeze saves failed on Pydantic objects; failure records retained. Corrected only model serialization, then captured unchanged baseline. Failure attempt excluded from scores. |
| I16 | Product | Conflicting original deadline remains unresolved. | C02 contains 23:59 and 23:50; Gold records INFO conflict. No invented correction. TASK05 is now completed under authorized scope; see I17 for direction and future-work boundary. |
| I17 | Product / Engineering | TASK05 authorized blind comparison completed; prior I09/I16 waiting status superseded. | Measurement PASS, locked AI primary candidate STOP. GATED recall47.98%, precision35.02%, unsupported BLOCKER3. No production integration; Product Lead decides any separately frozen future scope. |
| I18 | Engineering | Existing100-candidate cap rejects the entire C01 AI result. | RAW117 becomes GATED0, losing36 complete matches. Preserved actual behavior; no truncation, limit change or prompt tuning. |
| I19 | Product / Engineering | Fine-grained AI fragments do not fully match several frozen Gold units. | PARTIAL remains zero; missing details cannot be recombined into candidate full credit. Supported OUTSIDE_GOLD statements and subjective evidence judgements are disclosed. Gold remains immutable and no independent human adjudication is claimed. |
| I20 | Engineering | TASK06 resolves the 100-candidate all-or-nothing product failure. | More than100 is visible overflow, Stage2 uses50-item batches, and500 is an explicit hard ceiling. Completed batches and raw candidates are preserved for failed-batch retry; no silent truncation. |
| I21 | Product / Engineering | Actual local Codex is suitable for the loopback MVP but has no production availability contract. | Keep the adapter boundary and explicit provider status. TASK07 adds durable job/checkpoint state but does not select a production provider or add an account/OAuth/payment. |
| I22 | Engineering | Synchronous AI extraction exceeded the frontend rewrite connection window. | Start actual long-running extraction in the backend, return RUNNING immediately, and poll short session status requests until completion. TASK07 makes state restart-safe on one node; execution locks remain process-local. |
| I23 | Product | Stage2 semantic review reduces obvious noise but is not authoritative adjudication. | KEEP/REVIEW/DROP reasons and provenance remain visible. Every retained candidate is non-authoritative until separate human approval and whole-source confirmation. |
| I24 | Product / Engineering | PARTIALLY RESOLVED by TASK08; Vision/OCR and deployment remain unsupported. | TASK08 adds a narrow `generic_verifier` lane for closed deterministic PDF/MP4 plans. All other generic rules remain REVIEW/EXTERNAL and deployment still requires separate scope. |
| I25 | Engineering | TASK07 durable storage is not a distributed coordinator. | SQLite plus process-local locks support one backend process on one node. Multi-worker/multi-host safety requires a separately designed lease/queue boundary and is not claimed. |
| I26 | Engineering | A process can stop between a provider response and the next committed checkpoint. | Only committed checkpoints count as completed. Work after the latest committed boundary may repeat; already committed Stage1 and Stage2 batches do not repeat. |
| I27 | Product / Engineering | Runtime data now outlives a backend process. | Keep `FINAL_CHECK_DATA_DIR` on application-owned local storage, exclude it from Git, apply the one-hour safe TTL, and define production retention/backup in the deployment task. |
| I28 | Product / Engineering | The local planner has no production availability contract. | Keep `VerificationPlanner` as a provider boundary. Current ACTUAL evidence uses the existing ChatGPT-authenticated Codex CLI; no API key, paid service, OAuth or production provider was selected. |
| I29 | Engineering | Natural-language qualifiers and semantic targets can make a typed plan unsafe. | The deterministic gate downgrades conditional, ungrounded, qualified or unsupported plans to REVIEW_ONLY. Runtime ambiguity and parser/ffprobe failure also return REVIEW; READY is intentionally rare. |
| I30 | Product / Engineering | TASK08 actual passing evidence covers one exact public C01 excerpt, not a complete announcement. | The full C01/C02 attempts encountered existing Stage1 malformed output and one excerpt attempt encountered transient provider unavailability. Claim only the final passing exact-excerpt product E2E; no benchmark or universal-coverage claim. |
| I31 | Engineering | Typed checker scope is deliberately narrow. | Current automatic support is PDF/MP4 metadata and file constraints. Encrypted PDF behavior was covered through safe failure paths, but an actual encrypted PDF and multi-worker execution were not tested. |
| I32 | Engineering | RESOLVED in TASK08 corrective change: FILE_TYPE MP4 converted every ffprobe failure into VIOLATION. | Separate type inspection outcomes into MATCH, MISMATCH and REVIEW. Unavailable/timeout/malformed/execution failure is REVIEW; only a successful definite non-MP4 container is MISMATCH/VIOLATION. |
| I33 | Engineering | RESOLVED in TASK08 corrective change: extension-target file checks could PASS arbitrary bytes. | Presence/count/size and extension-targeted name checks require common PDF signature/parser or MP4 ffprobe trust before a definite result. A fake `.mp4` mandatory-only profile is regression-tested as REVIEW_REQUIRED, never READY. |
| I34 | Product / Operations | RESOLVED by the TASK09 zero-cost pivot: paid Railway and OpenAI API resources were canceled. | Use the existing authenticated Codex CLI and the ngrok Free account-assigned development domain. Never copy Codex/ngrok authentication into the repository or evidence. |
| I35 | Engineering | Public abuse controls have no trusted per-IP identity. | Use durable global and per-session hourly quotas plus a global concurrent workflow cap. `PER_IP_LIMIT = NOT_IMPLEMENTED`; do not trust forwarded headers without deployed-path verification. |
| I36 | Engineering | TASK09 remains a single-node SQLite runtime on the judging PC. | One Uvicorn worker is exposed through ngrok. Host/network availability, cloud failover, production SLA, DDoS resilience, large traffic, backups and distributed coordination are not provided. |
