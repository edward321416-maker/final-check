## Executed Actions

- Added the TASK07 restart-safe single-node runtime while preserving the TASK06 two-stage local AI flow and mandatory human confirmation.
- Persisted canonical session/profile/review/validation metadata and explicit extraction-job state in SQLite.
- Replaced TemporaryDirectory-only announcement/submission storage with configurable application-owned durable paths.
- Added Stage1, deterministic-gate, per-Stage2-batch and final checkpoints, startup recovery, idempotency, bounded explicit retry and safe TTL cleanup.
- Added 18 durability/recovery/security tests, including an actual backend process restart, and preserved separate standard-browser and one-run actual-Codex evidence.

## GSTACK & Skill Usage

- Used the activated repository execution policy and the user-requested github-task-flow skill. No new plugin, skill, account, OAuth grant, API key or paid service was acquired.
- Actual AI regression used the already authenticated local Codex CLI exactly once; authentication data and provider stderr were never persisted or committed.
- No plugin schema was acquired, so no Drive schema upload was required.
- Google Workspace API delivery remains unavailable. The major-task event is queued outside Git in the project AIExecution state directory and is not reported as synchronized.
- Overall task token usage is unknown; no exact token or savings claim is made.

## PR Status

- Repository: https://github.com/edward321416-maker/final-check.
- TASK07 baseline: 07cd3e6587efd47661f2e42f72574f569bce9094.
- GitHub task: https://github.com/edward321416-maker/final-check/issues/8.
- Branch: issue/8-restart-safe-durable-runtime.
- The delivery PR is intentionally kept OPEN / NOT MERGED for Product Lead review.

## Unresolved Issues / Next Steps

- The runtime is durable for one backend process on one node; multiple workers/nodes, cloud volumes and backup/restore are not tested or claimed.
- A provider response can repeat if the process stops before its next checkpoint commit; completed durable Stage1 and Stage2 batches do not repeat.
- Production AI provider selection, public deployment, Vision/OCR and generic submission verification remain outside TASK07.
- After TASK07 review and merge, the locked next priority is TASK08 Generic Verifier Engine.
